kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # This section maps the Windows localhost port to the Kind Node's port
  extraPortMappings:
  - containerPort: 30080
    hostPort: 30080
    protocol: TCP
